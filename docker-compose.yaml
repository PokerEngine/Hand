services:
    hand:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ./:/usr/local/project/src
        environment:
            - DOTNET_ENVIRONMENT=Development
