services:
    hand:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - .:/usr/local/project
        environment:
            - DOTNET_ENVIRONMENT=Development
