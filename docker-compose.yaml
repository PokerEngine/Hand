services:
    hand:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ./src:/usr/local/project/src
        environment:
            - DOTNET_ENVIRONMENT=Development
